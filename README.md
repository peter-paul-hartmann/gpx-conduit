# gpx-conduit

library to parse gpx files (see http://www.topografix.com/GPX/1/1). 
The implementation uses the streaming library of xml-conduit. The function 'Geo.GPX.Conduit.parseGPXFile' 
returns a GPX data structure representing the parsed gpx file.
